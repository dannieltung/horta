<%= simple_form_for [@product, @review] do |f| %>
  <%= f.input :content, placeholder: 'Your Rewview', label: false %>
  <%= f.input :rating, placeholder: 'Rating', label: false %>
  <%= f.submit "Review it!", class: 'btn btn-success' %>
<% end %>