<div class="new_all">
  <h1>New Transaction</h1>

  <%= simple_form_for [@user, @transaction] do |f| %>
    <%= f.association :product %>
    <%= f.input :quantity %>
    <%= f.input :book, as: :datetime, html5: true %>
    <%= f.submit class: 'btn btn-success' %>
  <% end %>

</div>
